# AXSG Language Server (VS Code)

Local VS Code client wrapper for the AXSG LSP host (`axsg-lsp`).

## Development

```bash
npm install
npx @vscode/vsce package
```

Install generated VSIX via VS Code command palette: `Extensions: Install from VSIX...`.
